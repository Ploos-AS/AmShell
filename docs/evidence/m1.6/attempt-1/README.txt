The first visible FS-UAE run used the initially generated bundle unchanged.
AmigaOS 2.04 stopped at the MakeDir line containing unsupported numbered
2>NIL: redirection. It produced neither native nor AmShell result files and did
not reach the qualification completion marker. Command-01-T01 is the raw guest
temporary command stream; the environment, CPU, profile and full emulator log
are preserved alongside it. The raw log is stored as deterministic gzip.
